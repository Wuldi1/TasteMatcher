{
  "name": "tastematcher",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webapi",
    "workers",
    "frontend",
    "common"
  ],
  "scripts": {
    "install:all": "npm install",
    "build:common": "npm run build --workspace=common",
    "build:webapi": "npm run build:common && npm run build --workspace=webapi",
    "build:workers": "npm run build:common && npm run build --workspace=workers",
    "dev:webapi": "npm run build:common && npm run dev --workspace=webapi",
    "dev:workers": "npm run build:common && npm run dev --workspace=workers"
  },
  "devDependencies": {
    "eslint": "^9.38.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "pnpm": "^10.20.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}